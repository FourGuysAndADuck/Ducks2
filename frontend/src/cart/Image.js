import React from "react";
import useFetch from '../useFetch';

const Image = (props) => {
    return (
        <div>


        <picture>
            <img src="/images%20(2).jpeg"  />
        </picture>
            )}

        </div>
    );
};

export default Image;